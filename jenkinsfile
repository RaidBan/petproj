	pipeline {
		agent {
			docker { image 'python:latest' }
		}
		stages {
			stage('Checkout') {
				steps {
					sh '''ls -la'''
					echo 'step Git Checkout'
				sh '''
					ls -la
					. petproj/bin/activate
					python3 --version
					python3 main.py
				'''
				
			}
		}
		
	}
}
